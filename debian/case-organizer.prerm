#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    systemctl stop case-organizer.service >/dev/null 2>&1 || true
    systemctl disable case-organizer.service >/dev/null 2>&1 || true
fi
exit 0

